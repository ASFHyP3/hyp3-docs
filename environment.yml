name: hyp3-docs
channels:
  - conda-forge
  - nodefaults
dependencies:
  - python=3.8
  - pip
  # For documentation
  - jinja2<3.1  # FIXME: Hack until mkdocs >1.2.3 released
  - mkdocs
  - mkdocs-material=6.2
  - mkdocs-material-extensions
  - hyp3_sdk=1.4.1  # also pinned in docs/using/sdk.md
  - asf_tools=0.3.3  # also pinned in docs/tools/asf_tools.md
  - requests
  - pip:
      # For documentation
      - mkdocs-asf-theme>=0.2.3
      - mkdocs-redirects
      - mkdocs-section-index
      - mkdocstrings
      - mkdocs-macros-plugin
